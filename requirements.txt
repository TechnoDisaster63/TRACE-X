# TRACE-X prototype requirements
#
# Runtime analysis uses only the Python standard library
# (email, hashlib, re, html.parser, urllib.parse, dataclasses, argparse,
# json, glob, pathlib, logging, uuid, textwrap, datetime, hashlib).
#
# pytest is required to run the test suite.
pytest>=7.4,<10
# Local-only MMDB reader for bundled DB-IP Lite Country data (no runtime network access).
maxminddb==2.8.2
