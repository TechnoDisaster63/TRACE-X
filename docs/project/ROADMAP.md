# Roadmap

- **IMPLEMENTED prototype:** inspection/baseline; provenance; M11; M12; BEC; M13; M14; M16 typed models; M17 offline geo/infrastructure intelligence; M18 advisory offline ML signal; tests/docs.
- **NEXT foundational:** report privacy/redaction/retention; stable forensic manifest; remove mutable module counters; atomic case writes; Python/Windows CI; fuzz/property testing.
- **FUTURE:** safe authenticated persistence/API/UI, recipient/asset context, optional external intelligence with SSRF controls, persistent campaign history, offline adapter interfaces, ML training/evaluation on modern corpora (the bundled M18 model is historical-data only).
- **NOT BEFORE HARDENING:** live enterprise integrations or production ML enforcement.
